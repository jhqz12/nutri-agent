# AGENTS.md — Codex 工作指令

## 项目是什么
这是一个「私人营养师 Agent」项目。目标是把营养计算、配餐、微量元素校验、补剂决策、训练计划做成一个可运行的应用（前台给用户看总览，后台给用户调参数）。

## 唯一真源（必读，先读再动手）
**`营养师Agent_完整指令v2.md`** —— 全部业务逻辑、数据库结构、计算公式、校验规则都在这一份文件里。
- 动手写代码前必须完整读完该文件。
- 任何业务规则与该文件冲突时，以该文件为准；要改规则先在该文件里改，再改代码。
- 该文件中的营养数值（食材库 2.1、标准库 2.3）**禁止篡改**，只能按文件原值录入。

## 语言与风格
- 全部界面、注释、文档用**简体中文**。
- 回复用户也用简体中文。

## 架构红线（来自指令文件第 0 章）
1. 后台一切可调：食材/补剂/微量标准/公式/动作/倾向规则，全部做成可编辑数据（JSON/SQLite/配置文件），不写死在代码里。
2. 前台只看结果：热量表 + 微量元素表 + 每日总览，不暴露内部计算参数。
3. 分量现实化：鸡蛋按个、奶按袋、饭按 10g 取整，禁止输出「1.75 份」。
4. 手动覆盖永远优先于公式计算。
5. 补剂合并计算：含热量的补剂（鱼油）计入热量，全部补剂计入微量元素总账，超 UL 必须红线提示。

## 当前已知数据缺口（代码里留占位，别编造）
- 用户年龄已确认为 **27 岁**，写入用户档案，后台保留修改入口。
- 补剂库 S01–S18 多数成分数值标了「待补数值」，录入界面要支持用户后补。

## 开发顺序建议
1. 先搭数据层：六库（食材/补剂/微量标准/公式/动作/倾向）的结构化存储 + 后台编辑。
2. 再做计算引擎：热量公式 → 宏量分配 → 随机菜单（倾向加权）→ 微量元素汇总 → 补剂决策。
3. 最后做前台：每日总览 + 双表格 + 趋势表。
4. 每完成一个模块，用指令文件第 2.4 章的示例（108kg/176cm/27岁/活动系数1.55）做自检验算。

## 验证标准
- 代入用户档案（108kg/176cm/27岁/活动系数1.55），输出必须等于：BMR 2050 / TDEE 3178 / 蛋白172.8g / 脂肪97.2g / 训练日碳水270g、总热量2646kcal / 休息日碳水216g、总热量2430kcal。
- 盐和钠按等价规则显示：个人硬上限为钠2000mg/日，前台同时显示约等于食盐5g。
- 葫芦巴 2 粒（锌 40mg）当日，锌镁硼硒片必须被锁死并给出提示。
