# 衡动设计规范

## 视觉方向

设计方向为“林雾健康工作台”：深森林色侧栏提供稳定边界，浅薄荷灰作为安静画布，白色信息面用于日常操作，暖黄和珊瑚只标记能量、提醒与风险。整体参考用户提供的健康、学习和数据工作台截图，但不照搬营销式插画或过度卡片化布局。

## 主题

| 主题 | 页面背景 | 主色 | 深主色 | 侧栏 |
| --- | --- | --- | --- | --- |
| 林雾薄荷 | `#eef6f2` | `#5d9b87` | `#2f6f5c` | `#1d3d35` |
| 海盐湖蓝 | `#eef6f8` | `#5d94a5` | `#326a7b` | `#203f49` |
| 晨光鼠尾草 | `#f4f5ec` | `#879665` | `#5d6e3f` | `#35412c` |
| 雾紫晚樱 | `#f5f2f7` | `#8879a7` | `#5f5083` | `#39334b` |
| 玄夜黑金 | `#11110f` | `#c7a65c` | `#d6b96f` | `#0c0c0b` |

默认主题为林雾薄荷。玄夜黑金使用近黑画布、暖白正文和低饱和金色操作色，面向夜间低亮环境，不采用纯黑纯白的强刺激组合。主题选择保存在浏览器本地，刷新后继续生效。所有主题使用同一套状态含义和组件结构。

## 布局

- 桌面侧栏宽 220 像素，内容区最大宽度 1500 像素。
- 首页首屏顺序为：页面标题、四项关键数字、今日提示、四餐与风险、时间轴。
- 大块内容使用完整工作面，不在面板内继续套装饰卡片。
- 早餐、午餐、晚餐、加餐保持四个清楚的大区，内部再列食材。
- 专业表格保持紧凑，操作列固定在信息行附近。

## 组件

- 主面板圆角 14 像素，控件圆角 9 像素，小状态标签使用胶囊形。
- 主要按钮使用深主色和白字；次要按钮使用浅表面与边框。
- 图标统一使用 Lucide，不用表情符号代替功能图标。
- 重要状态同时显示图标、文字和颜色，不能只依赖颜色。

## 字体与密度

- 中文使用微软雅黑、苹方和系统无衬线字体。
- 页面标题 28 至 34 像素，板块标题 17 至 20 像素，正文 12 至 14 像素。
- 重要数字使用更清楚的字重和等宽数字；不使用夸张营销字号。
- 工作区垂直间距以 18、24、32 像素为主要节奏。

## 动效

- 页面切换时只对当前一级内容做一次 360 毫秒的轻微上移与淡入。
- 主题切换使用浏览器即时换色，不增加复杂过渡。
- 不使用滚动劫持、循环装饰动画或列表逐项长时间播放。
- 开启“减少动态效果”后取消位移、缩放和入场动画。

## 响应式

- `390×844` 手机端使用底部六项导航，桌面侧栏隐藏。
- 手机表单改为单列，按钮和输入框不小于 44 像素。
- 页面本身不得横向滚动；宽表格只在自己的容器内滚动。
- 页面底部为固定导航和安全区域预留空间。
